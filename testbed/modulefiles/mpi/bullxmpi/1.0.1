#%Module

# Software description
set version     "1.0.1"

# Loads software's environment
setenv          CCC_MPI_TYPE    "openmpi"
#setenv          OMPI_MCA_orte_process_binding "core"

# Common
if [file exists $env(MODULEPATH)/mpi/bullxmpi/.common] {
    source $env(MODULEPATH)/mpi/bullxmpi/.common
}

